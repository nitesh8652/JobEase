/* Minimal entry to select Node or browser implementation for `debug` */
/* This file is added to satisfy packages requiring `./src/index.js` */

/* Environment detection: prefer browser when a DOM is present */
if (typeof window !== 'undefined' && typeof window.document !== 'undefined') {
  module.exports = require('./browser');
} else {
  module.exports = require('./node');
}
